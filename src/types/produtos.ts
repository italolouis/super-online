export interface IProdutos {
    id: string
    descricao : string,
    valor : number,
    imagem : string,
    prazoValidade : string,
    estoque : number,
    quantidade : number,
    selecionado: boolean,
    adicionado: boolean,
}